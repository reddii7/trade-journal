VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Netlify → Site → Environment variables → scope "Functions" or "All"
# IG Index: My IG → Settings → API keys (create separately for demo vs live if needed)
IG_API_KEY=
# Use either pair (function accepts both naming styles):
IG_USERNAME=
IG_PASSWORD=
# IG_USER=          # alias for IG_USERNAME
# IG_PASS=          # alias for IG_PASSWORD
